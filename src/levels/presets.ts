/**
 * 关卡初始化时写入的模板文件内容（development-refinement.md §3 `levels/presets.ts`）。
 *
 * 用途：第一章「创世纪元」是「修复损坏的时间线」，预置文件承担叙事铺垫 ——
 * 玩家进入关卡时，工作区里躺着若干「时间线残片」，接着用真实 git 命令把它们归档。
 *
 * ⚠️ 两条硬约束：
 *   1. **保持精简**（§14：LightningFS 大仓库有性能问题）。每个文件都是几句话，
 *      不做长篇设定文案 —— 这些内容会真实写入 `/repo` 并被 git 追踪。
 *   2. 内容与路径**用中文**，路径保持英文（`README.md` / `notes/*.md`），
 *      与仓库既有笔记风格一致。
 *
 * ⚠️ 关于「文档换行」：LightningFS 与 git 按字节存取，换行符会进入 blob。
 * 本文件统一用 `\n`，`targetState` 的 `file` 比对也会做 CRLF 归一，
 * 因此玩家在任意平台编辑后内容匹配都不会因行尾差异误判。
 */

/**
 * 各关共用的项目说明文件。
 *
 * ⚠️ 不要把它写进「要求起始工作区干净」的关卡 —— 带 `files` 的 `reset()` 之后
 * 文件处于未追踪状态，`workdirClean: { value: true }` 会立刻判失败（实测结论）。
 * 1-2「第一次快照」需要玩家先看到待归档内容，正是这种「故意不干净」的用途。
 */
export const README_MD = `# 时间线档案

这条时间线曾被人为抹除。所幸碎片还在：

- 目录里散落着未归档的文本
- 归档区（仓库）空空如也
- 没有任何快照记录可言

你的任务：把碎片按顺序收拢，让时间线重新具备向前延伸的能力。
`;

/**
 * 1-1「时间线初始化」的预置文件。
 *
 * ⚠️ 1-1 是本关唯一要求「起始干净」的经典场景的反面 —— 它的目标是「仓库已建立」，
 * 因此用 `init: {}`（无 files），让玩家从**完全空白**开始执行 `git init`。
 * 仅当关卡需要叙事铺垫时才引入预置文件，1-1 用这段文本作为「档案封面」。
 */
export const TIMELINE_FRAGMENT = `时间线残片 · 编号 001

观测记录：本地档案库（Local Repository）尚未建立。
在档案库出现之前，任何改动都无法被记住。
`;

/** 1-2「第一次快照」：待归档的观测记录，玩家需 add + commit 收拢它 */
export const OBSERVATION_LOG = `观测记录 · 第一次快照

工作区已捕获一段文本，但它还停留在「已修改、未归档」的状态。
先把它送入暂存区，再正式归档为第一个快照。
`;

/** 1-3「三态之谜」：一份待归档的残片，玩家需走完 工作区 → 暂存区 → 仓库 三段 */
export const THREE_STATES_NOTE = `三态观测笔记

同一份内容，在时间线里会经历三种身份：
  1. 工作区 —— 你正在编辑的形态
  2. 暂存区 —— 已被选中、等待归档的形态
  3. 本地仓库 —— 已归档、成为历史的形态

让这份笔记完整走完三段。
`;

/** 1-4「历史之链」：残余档案，玩家需在其上连续归档，形成有父提交的历史链 */
export const CHAIN_START = `时间线修复日志 · 起点

此处是链条的第一环。
每一份归档都会指向前一份，从而把孤立的时间点串成历史。
`;

/** 1-4 的第二份内容：玩家改写后再次归档，链条因此延长 */
export const CHAIN_CONTINUATION = `时间线修复日志 · 延续

第二环已记录。
现在这条时间线拥有了「父提交」的概念 —— 历史不再是孤立的快照。
`;
