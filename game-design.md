# Git 时间旅人 —— 游戏设计文档（GDD）

> 版本：v1.0（草案）
> 日期：2026-04-12
> 状态：待评审
> 基于仓库内 8 篇 Git 学习笔记设计：《Git基础概念》《Git基础操作》《Git分支管理》《Git远程操作》《Git撤销操作》《Git标签管理》《Git最佳实践》《frontend-changes》

---

## 1. 游戏概述

### 1.1 一句话定位
一款**纯浏览器、单机、时间旅行/平行宇宙主题**的 Git 学习闯关游戏，玩家扮演「时间旅人」，通过执行真实的 Git 命令修复被破坏的「时间线」，在娱乐中系统掌握从基础到进阶的 Git 技能。

### 1.2 核心概念
- **时间线（Timeline）**：每个关卡是一个被破坏/错乱的 Git 仓库（时间线），存在一个或多个「时间悖论」待修复。
- **时间旅人（玩家）**：唯一主角，通过输入/拼接 Git 命令来修复历史、合并平行宇宙、撤销错误提交。
- **时间悖论**：每个关卡的目标问题（如错误提交、冲突合并、丢失的分支、打错的标签）。
- **时间锚点（Anchor）**：关卡预设的正确中间状态，用于判定玩家是否走在正确路径上。

### 1.3 设计目标
1. **教育性**：完整覆盖 8 篇笔记的全部知识点，并映射为可量化的关卡目标。
2. **娱乐性**：时间旅行叙事 + 即时可视化的提交历史/分支图谱 + 音效/动效反馈。
3. **低门槛**：纯 Web 运行、无需安装，混合式输入（前期拼接、后期自由输入）降低学习曲线。
4. **可度量**：以「减少错误/重试」为核心的评分体系，鼓励理解而非试错。

### 1.4 目标受众
全覆盖，分难度梯度：新手（入门）、进阶（分支/合并）、高阶（远程/撤销/标签/最佳实践）。

---

## 2. 世界观与叙事

- 玩家是「时间线管理局」的旅人，负责修复因各种事故崩坏的时间线。
- 每个章节对应一个平行宇宙，其「历史」用 Git 提交历史（DAG）可视化呈现。
- 修复时间线 = 通过 Git 命令让仓库达到预期的目标状态。
- 章节名与主题映射：
  - 第一章「创世纪元」—— Git 基础概念
  - 第二章「日常秩序」—— Git 基础操作
  - 第三章「平行宇宙」—— Git 分支管理
  - 第四章「星际连接」—— Git 远程操作
  - 第五章「时空回溯」—— Git 撤销操作
  - 第六章「历史锚点」—— Git 标签管理
  - 第七章「法则之书」—— Git 最佳实践
  - 终章「大统一」—— 综合实战

---

## 3. 核心玩法

### 3.1 玩法循环
1. **观察**：查看当前仓库状态（`status` 可视化面板、提交历史 DAG、分支图谱、工作区文件树）。
2. **诊断**：阅读关卡提示与「时间悖论」描述，判断需要什么操作。
3. **执行**：通过输入/拼接 Git 命令，真实执行（isomorphic-git 或系统 git）。
4. **验证**：系统检测是否达成「时间锚点」或最终目标状态。
5. **反馈**：提交历史/图谱动态变化，成功音效与动效；失败给出错误提示与线索。

### 3.2 输入方式（混合式，随难度演进）
- **前期（第 1–2 章）**：命令拼接 —— 将分散的命令片段（`git`、`commit`、`-m`、`add`、`.` 等）拖拽/点击拼接成完整命令。
- **过渡（第 3–4 章）**：半拼接半输入 —— 提供命令骨架 + 关键参数填空。
- **后期（第 5–7 章及终章）**：自由输入 —— 完整键入命令，支持别名与常见参数。

### 3.3 关卡内辅助
- **提示系统**：分级提示（方向提示 → 命令提示 → 完整答案），使用会扣分。
- **命令参考卡**：解锁式笔记摘要，关联对应 `.md` 原文要点。
- **状态可视化**：提交历史 DAG、分支/标签、工作区/暂存区三态图。

---

## 4. 关卡列表

> 每关标注：目标、关联笔记章节、输入方式、难度（★ 1–5）。

### 第一章「创世纪元」—— Git 基础概念
| 关卡 | 名称 | 目标 | 关联 | 输入 | 难度 |
|---|---|---|---|---|---|
| 1-1 | 时间线初始化 | 用 `git init` 创建仓库 | 基础概念·仓库 | 拼接 | ★ |
| 1-2 | 第一次快照 | `add` + `commit` 生成首个提交 | 基础概念·快照/提交 | 拼接 | ★ |
| 1-3 | 三态之谜 | 理解工作区/暂存区/仓库三态 | 基础概念·三态 | 拼接 | ★★ |
| 1-4 | 历史之链 | 识别提交对象与父提交关系 | 基础概念·对象模型 | 拼接 | ★★ |

### 第二章「日常秩序」—— Git 基础操作
| 关卡 | 名称 | 目标 | 关联 | 输入 | 难度 |
|---|---|---|---|---|---|
| 2-1 | 状态感知 | `status` 查看当前状态 | 基础操作·status | 拼接 | ★ |
| 2-2 | 记录变更 | `diff` 查看具体改动 | 基础操作·diff | 拼接 | ★★ |
| 2-3 | 日志回溯 | `log` 阅读提交历史 | 基础操作·log | 拼接 | ★★ |
| 2-4 | 移除与忽略 | `rm` + `.gitignore` 管理文件 | 基础操作·rm/ignore | 拼接 | ★★ |

### 第三章「平行宇宙」—— Git 分支管理
| 关卡 | 名称 | 目标 | 关联 | 输入 | 难度 |
|---|---|---|---|---|---|
| 3-1 | 分裂宇宙 | `branch` 创建分支 | 分支管理·创建 | 半拼 | ★★ |
| 3-2 | 穿越宇宙 | `checkout`/`switch` 切换分支 | 分支管理·切换 | 半拼 | ★★ |
| 3-3 | 合并宇宙 | `merge` 合并分支 | 分支管理·合并 | 半拼 | ★★★ |
| 3-4 | 冲突消解 | 解决合并冲突 | 分支管理·冲突 | 半拼 | ★★★★ |
| 3-5 | 变基重写 | `rebase` 整理历史 | 分支管理·rebase | 半拼 | ★★★★ |
| 3-6 | 分支博弈 | merge vs rebase 场景选择 | 分支管理·最佳实践 | 半拼 | ★★★★ |

### 第四章「星际连接」—— Git 远程操作
| 关卡 | 名称 | 目标 | 关联 | 输入 | 难度 |
|---|---|---|---|---|---|
| 4-1 | 建立航道 | `remote add` 关联远程 | 远程操作·remote | 半拼 | ★★★ |
| 4-2 | 传送数据 | `push` 推送分支 | 远程操作·push | 半拼 | ★★★ |
| 4-3 | 接收数据 | `fetch`/`pull` 拉取更新 | 远程操作·fetch/pull | 半拼 | ★★★ |
| 4-4 | 克隆宇宙 | `clone` 复制仓库 | 远程操作·clone | 半拼 | ★★★ |
| 4-5 | 协作冲突 | 多人推送冲突处理 | 远程操作·协作 | 半拼 | ★★★★★ |

### 第五章「时空回溯」—— Git 撤销操作
| 关卡 | 名称 | 目标 | 关联 | 输入 | 难度 |
|---|---|---|---|---|---|
| 5-1 | 修正笔误 | `commit --amend` 修正提交 | 撤销操作·amend | 自由 | ★★★ |
| 5-2 | 撤销暂存 | `reset` 从暂存区撤销 | 撤销操作·reset | 自由 | ★★★ |
| 5-3 | 丢弃改动 | `checkout --`/`restore` 丢弃工作区 | 撤销操作·checkout | 自由 | ★★★★ |
| 5-4 | 安全反转 | `revert` 生成反向提交 | 撤销操作·revert | 自由 | ★★★★ |
| 5-5 | 危险与安全 | reset vs revert 抉择 | 撤销操作·对比 | 自由 | ★★★★★ |
| 5-6 | 时间跳跃 | `reset --hard` 后果与补救 | 撤销操作·reflog | 自由 | ★★★★★ |

### 第六章「历史锚点」—— Git 标签管理
| 关卡 | 名称 | 目标 | 关联 | 输入 | 难度 |
|---|---|---|---|---|---|
| 6-1 | 轻量锚点 | 创建 lightweight tag | 标签管理·轻量标签 | 自由 | ★★★ |
| 6-2 | 注解锚点 | 创建 annotated tag | 标签管理·注解标签 | 自由 | ★★★ |
| 6-3 | 列出与查看 | `tag` 列表与查看 | 标签管理·查看 | 自由 | ★★★ |
| 6-4 | 推送锚点 | 推送标签到远程 | 标签管理·push tag | 自由 | ★★★★ |
| 6-5 | 版本发布 | 用标签标记发布版本 | 标签管理·语义化版本 | 自由 | ★★★★ |

### 终章「大统一」—— 综合实战
| 关卡 | 名称 | 目标 | 关联 | 输入 | 难度 |
|---|---|---|---|---|---|
| F-1 | 崩坏时间线 | 综合修复：分支+合并+冲突+撤销 | 全部 | 自由 | ★★★★★ |
| F-2 | 完整交付 | 从 init 到 tag 的完整流程 | 全部 | 自由 | ★★★★★ |

---

## 5. 评分规则

> 核心原则：**奖励理解、惩罚试错**（减少错误/重试）。

### 5.1 单关评分（满分 100）
| 维度 | 权重 | 说明 |
|---|---|---|
| **正确性** | 40% | 达成目标状态（时间锚点全部命中） |
| **效率** | 30% | 命令次数越少越高；含冗余/无关命令则扣分 |
| **稳定性** | 30% | 错误与重试次数；错误命令（语法/语义）扣分 |

- **基础分**：达成目标即得正确性满分（40）。
- **效率分**：`max(0, 30 − (实际命令数 − 最优命令数) × 系数)`。
- **稳定性分**：`max(0, 30 − 错误次数 × 扣分值 − 提示次数 × 扣分值)`。
- 提示扣分：方向提示 −5，命令提示 −10，完整答案 −20。

### 5.2 星级评定（单关）
| 星级 | 分数 | 条件 |
|---|---|---|
| ★★★ | 90–100 | 零错误、命令数最优、无提示 |
| ★★ | 70–89 | 少量错误/1 次方向提示 |
| ★ | 50–69 | 多次重试或使用命令提示 |

### 5.3 全局统计
- **总得分**：所有关卡得分累加。
- **时空能量（经验）**：随章节解锁增长，用于解锁提示额度。
- **完美通关**：全部关卡 ★★★。
- **成就系统**：单机成就（如「零回溯大师」「一次成型」「无提示通关」）。

### 5.4 反作弊/边界
- 直接复制目标仓库内容而非执行命令 → 不计分（检测目标状态 vs 命令序列）。
- 使用危险命令（`reset --hard` 等）若无目标要求但未造成不可逆破坏，视为「冒险行为」给予额外提示警示。

---

## 6. 技术选型

> 依据：纯 Web、单机、无需安装、真实 git 执行、无偏好可推荐。

### 6.1 前端框架与构建
| 项 | 选型 | 理由 |
|---|---|---|
| 框架 | **React + TypeScript** | 生态成熟、类型安全、组件化适合状态可视化 |
| 构建 | **Vite** | 启动/构建快、HMR 友好、现代浏览器支持 |
| UI 样式 | **Tailwind CSS** | 快速迭代、风格统一、响应式 |
| 状态管理 | **Zustand**（轻量） | 仓库状态/关卡进度管理简单，无需重型 Redux |
| 路由 | **React Router** | 章节/关卡路由 |

### 6.2 Git 执行引擎（核心）
| 项 | 选型 | 理由 |
|---|---|---|
| 引擎 | **isomorphic-git** | 纯 JS、可在浏览器运行、API 化（`git.init/add/commit/...`）、无需后端、跨平台一致 |
| 底层存储 | **LightningFS（in-browser 虚拟文件系统）** | 配合 isomorphic-git 在浏览器内持久化仓库 |
| 备选 | 若需完整兼容性可**调用系统 git**（Node/WASM 桥接），但会牺牲「无需安装」的纯 Web 体验 | 仅作为扩展方案 |
| 命令解析 | **自定义命令解析器** | 将拼接/输入的命令映射到 isomorphic-git API 调用 |

### 6.3 可视化
| 项 | 选型 | 理由 |
|---|---|---|
| 提交历史 DAG | **自绘 Canvas/SVG**（或 d3 + dagre 布局） | 清晰展示提交/分支/标签关系 |
| 三态图 | **自绘组件** | 工作区/暂存区/仓库状态 |
| 动效/音效 | **Framer Motion + Web Audio（合成音效）** | 时间旅行反馈，无外部资源依赖 |

### 6.4 关卡与内容数据
- 关卡配置以 **JSON/YAML Schema** 定义（目标状态、命令序列、提示、评分参数），便于扩展。
- 笔记知识点 → 关卡映射表由 `other/frontend-changes.md` 的模块化拆分对应。

### 6.5 持久化
- 关卡进度、得分、成就 → **localStorage / IndexedDB**（单机无后端）。
- 仓库快照 → LightningFS + IndexedDB 持久化。

### 6.6 测试
| 层 | 选型 |
|---|---|
| 单元测试 | **Vitest** |
| 组件测试 | **Testing Library** |
| 命令执行集成测试 | isomorphic-git 测试用例（断言目标状态） |

### 6.7 技术架构图

```
┌────────────────────────────────────────────────────────┐
│                     浏览器（纯 Web）                      │
│                                                          │
│  ┌─────────────┐   ┌──────────────┐   ┌─────────────┐  │
│  │ React UI    │   │ 命令解析器    │   │ 可视化层     │  │
│  │ (关卡/输入/  │◄─▶│ (拼接/输入→API)│◄─▶│ DAG/三态图   │  │
│  │  反馈/评分)  │   │              │   │ /动效音效    │  │
│  └──────┬──────┘   └──────┬───────┘   └─────────────┘  │
│         │                 │                             │
│         ▼                 ▼                             │
│  ┌─────────────────────────────────────────────────┐   │
│  │         isomorphic-git + LightningFS             │   │
│  │        （真实 Git 执行 / 虚拟文件系统）             │   │
│  └─────────────────────────────────────────────────┘   │
│         │                                               │
│         ▼                                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │      IndexedDB / localStorage（进度/快照持久化）  │   │
│  └─────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────┘
```

---

## 7. 界面与交互设计（概要）

### 7.1 主界面布局
- **顶部**：章节导航 + 时空能量/得分 + 成就入口。
- **左侧**：关卡任务面板（目标、时间悖论描述、提示按钮）。
- **中央**：仓库可视化（提交历史 DAG / 三态图 / 文件树）。
- **底部**：命令输入区（拼接区 or 输入框）+ 执行反馈（成功/错误 + 线索）。

### 7.2 关键交互
- 执行命令后 DAG 实时动画更新（提交节点生长、分支合并动效）。
- 错误命令触发「时间震荡」动效 + 错误解释。
- 达成目标触发「时间线修复」过场动画。

---

## 8. 内容映射表（笔记 ↔ 关卡）

| 笔记文件 | 覆盖章节/关卡 |
|---|---|
| Git基础概念 | 第一章 1-1 ~ 1-4 |
| Git基础操作 | 第二章 2-1 ~ 2-4 |
| Git分支管理 | 第三章 3-1 ~ 3-6 |
| Git远程操作 | 第四章 4-1 ~ 4-5 |
| Git撤销操作 | 第五章 5-1 ~ 5-6 |
| Git标签管理 | 第六章 6-1 ~ 6-5 |
| Git最佳实践 | 贯穿各章（merge/rebase、reset/revert、语义化版本等） |
| frontend-changes | 内容模块化依据 + 终章综合实战 |

---

## 9. 里程碑与范围

| 阶段 | 范围 | 交付 |
|---|---|---|
| M1 | 核心引擎 + 第 1–2 章 | 可玩原型（拼接输入 + DAG 可视化） |
| M2 | 第 3–4 章 + 半拼输入 | 分支/远程可视化 |
| M3 | 第 5–7 章 + 自由输入 + 评分系统 | 完整评分/成就 |
| M4 | 终章 + 打磨（动效/音效/提示系统） | 可发布版本 |

---

## 10. 风险与待确认项

| 风险/问题 | 说明 | 状态 |
|---|---|---|
| isomorphic-git 覆盖度 | 部分命令（`reflog`、`rebase` 复杂场景）支持有限 | ✅ 已确认：采用**混合引擎**。主引擎用 isomorphic-git，对覆盖不足的命令（如 `reflog`、复杂 `rebase`）接入系统本地 Git 作为 fallback，结果统一规范化为同一数据模型 |
| 命令解析复杂度 | 自由输入需健壮的 Git 命令解析器 | ✅ 已确认：引入**成熟解析库**（如 `@gitbeaker`、`simple-git` 等命令封装/解析层），限定支持的命令语法集，非法或越界命令给出友好提示 |
| `reset --hard` 破坏性 | 需在沙箱内安全模拟与可恢复 | ✅ 已确认：采用 **reflog 可恢复机制**。每次命令前自动记录快照，`reset --hard` 后可通过「时间回溯/reflog」节点撤销，模拟真实 reflog 语义，保证任意操作可恢复 |
| 评分「最优命令数」定义 | 需为每关预定义最优解或启发式 | ✅ 已确认：采用**区间阈值**评分。为每关预定义最优命令数区间（如 3–5 步），落在区间内得满分，超出按梯度扣分，避免「唯一最优解」的僵化判定 |

---

> 本文档为草案。第 10 节「风险与待确认项」已全部确认（详见各条「已确认」说明），可据此进入开发细化阶段。
